# Supabase (Project Settings > API)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
# Supabase > Connect > Session pooler URI (postgresql://...)
SUPABASE_DB_URL=

# Google Cloud console - enable: Places API (New), Geocoding API, Routes API
GOOGLE_MAPS_API_KEY=

# For the enrichment pipeline only (not needed to run the app)
ANTHROPIC_API_KEY=
